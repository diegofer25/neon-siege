{
  "name": "@neon-siege/server-cf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply neon-siege-db --local",
    "db:migrate:preview": "wrangler d1 migrations apply neon-siege-db --remote --env preview",
    "db:migrate:prod": "wrangler d1 migrations apply neon-siege-db --remote",
    "db:studio:local": "wrangler d1 execute neon-siege-db --local --command 'SELECT 1'",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "resend": "^6.9.2",
    "stripe": "^20.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.80.0",
    "typescript": "^5.6.0"
  }
}
